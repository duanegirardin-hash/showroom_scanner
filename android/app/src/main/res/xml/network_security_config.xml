<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Phase 3 PC Receiver uses HTTP on the private LAN only. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
